# APP_ENV currently only affects rate/message limits in entitlements.ts and ratelimits.ts
APP_ENV=dev

# --- Required for the web app (local pnpm dev and Vercel) ---
# generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=ad099fe99d937708dad7d8b462c3e45e

# https://vercel.com/docs/postgres (Neon). Required to sign in and save cloud chats.
POSTGRES_URL=postgresql://neondb_owner:npg_HO2WoeEF6IRL@ep-curly-bar-a70dvw0u-pooler.ap-southeast-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# https://serper.dev — live news, stocks, and web lookups (webSearch / searchNews tools).
# Copy this file to .env.local (or set it in the Vercel project) and paste your key.
# Do not commit the real key.
SERPER_API_KEY=

# --- Vercel AI Gateway (public web deploy) ---
# On Vercel, leave this empty: the app uses OIDC automatically when process.env.VERCEL is set.
# A Vercel account credit card may still be required to unlock Gateway free credits:
# https://vercel.com/d?to=%2F%5Bteam%5D%2F%7E%2Fai%3Fmodal%3Dadd-credit-card
# Optional off-Vercel (or local cloud models): paste a Gateway key from https://vercel.com/ai-gateway
# Leave empty for school/local LM Studio (`pnpm dev`).
AI_GATEWAY_API_KEY=

# --- Local / school lab only (ignored on Vercel when Gateway is active) ---
LMSTUDIO_BASE_URL=http://10.118.0.111:1234/v1 #change to http://127.0.0.1:1234/v1 for locally running lmstudio model
LMSTUDIO_API_KEY="Anything"
LMSTUDIO_MODEL="qwen/qwen3-vl-4b:2"

# --- Optional ---
# https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# https://vercel.com/docs/redis
REDIS_URL=****
